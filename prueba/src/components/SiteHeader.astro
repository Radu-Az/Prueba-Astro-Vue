---
/*
  Header principal:
  - Reutilizable en todas las paginas.
  - Marca ruta activa.
  - Permite cambiar idioma y tema.
*/
import ThemeToggleIsland from "./ThemeToggleIsland.vue";
import type { AppLang } from "../i18n/languages";
import { getLanguageSwitchLinks, normalizePathname, stripLanguagePrefix, toLocalizedPath } from "../i18n/languages";

interface HeaderCopy {
  tagline: string;
  openMenu: string;
  nav: {
    home: string;
    services: string;
    contact: string;
  };
  language: {
    es: string;
    en: string;
    pt: string;
    ca: string;
    fr: string;
    eu: string;
  };
}

interface SiteHeaderProps {
  lang: AppLang;
  copy: HeaderCopy;
}

const { lang = "es", copy } = Astro.props as SiteHeaderProps;

const currentPathname = normalizePathname(Astro.url.pathname);
const basePathname = stripLanguagePrefix(currentPathname);

const navLinks = [
  { label: copy.nav.home, href: toLocalizedPath("/", lang) },
  { label: copy.nav.services, href: toLocalizedPath("/servicios", lang) },
  { label: copy.nav.contact, href: toLocalizedPath("/contacto", lang) },
];

const languageLabelByCode = {
  es: copy.language.es,
  en: copy.language.en,
  pt: copy.language.pt,
  ca: copy.language.ca,
  fr: copy.language.fr,
  eu: copy.language.eu,
};

const languageSwitchLinks = getLanguageSwitchLinks(currentPathname).map((languageLink) => ({
  ...languageLink,
  label: languageLabelByCode[languageLink.code],
}));

const currentLanguageLabel = languageLabelByCode[lang];
---

<header class="sticky top-0 z-50 border-b border-slate-200/80 bg-white/85 backdrop-blur-xl transition-colors dark:border-slate-800/70 dark:bg-slate-950/85">
  <div class="mx-auto flex max-w-6xl items-center gap-4 px-6 py-4 lg:px-8">
    <a href={toLocalizedPath("/", lang)} class="inline-flex items-center gap-3 text-slate-900 dark:text-white">
      <span class="h-2.5 w-2.5 rounded-full bg-cyan-500 shadow-[0_0_0_7px_rgba(6,182,212,0.16)] dark:bg-cyan-300 dark:shadow-[0_0_0_7px_rgba(34,211,238,0.2)]"></span>
      <span>
        <span class="block text-base font-semibold leading-none">Orbit Web Studio</span>
        <span class="mt-1 hidden text-[11px] font-medium uppercase tracking-[0.2em] text-slate-500 sm:block dark:text-slate-400">
          {copy.tagline}
        </span>
      </span>
    </a>

    <nav class="ml-auto hidden items-center gap-8 md:flex" aria-label={copy.nav.home}>
      {
        navLinks.map((navigationLink) => {
          const isCurrentPage = basePathname === stripLanguagePrefix(normalizePathname(navigationLink.href));

          return (
            <a
              href={navigationLink.href}
              class:list={[
                "relative py-1 text-sm font-medium transition after:absolute after:bottom-[-7px] after:left-0 after:h-0.5 after:w-full after:origin-left after:rounded-full after:bg-cyan-500 after:transition-transform after:duration-300 dark:after:bg-cyan-300",
                isCurrentPage
                  ? "text-slate-900 after:scale-x-100 dark:text-white"
                  : "text-slate-600 after:scale-x-0 hover:text-slate-900 hover:after:scale-x-100 dark:text-slate-300 dark:hover:text-white",
              ]}
              aria-current={isCurrentPage ? "page" : undefined}
            >
              {navigationLink.label}
            </a>
          );
        })
      }
    </nav>

    <ThemeToggleIsland client:load class="hidden md:inline-flex" />

    <details class="group relative ml-3 hidden md:block">
      <summary class="inline-flex list-none cursor-pointer items-center gap-2 rounded-full border border-slate-300 bg-white/90 px-3 py-2 text-slate-700 marker:content-none dark:border-slate-700 dark:bg-slate-900/90 dark:text-slate-200">
        <span class="text-[11px] font-semibold tracking-[0.12em]">{currentLanguageLabel}</span>
        <svg
          class="h-3.5 w-3.5 text-slate-400 transition duration-200 group-open:rotate-180"
          viewBox="0 0 20 20"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="m6 8 4 4 4-4" />
        </svg>
      </summary>

      <div class="absolute right-0 mt-3 w-40 rounded-2xl border border-slate-300/80 bg-white/95 p-2 shadow-xl shadow-slate-900/10 dark:border-slate-700/80 dark:bg-slate-900/95 dark:shadow-slate-950/70">
        {
          languageSwitchLinks.map((languageLink) => (
            <a
              href={languageLink.href}
              class:list={[
                "block rounded-lg px-3 py-2 text-xs font-semibold tracking-wide transition",
                lang === languageLink.code
                  ? "bg-cyan-100 text-cyan-700 dark:bg-cyan-500/20 dark:text-cyan-200"
                  : "text-slate-600 hover:bg-slate-100 hover:text-slate-900 dark:text-slate-300 dark:hover:bg-slate-800 dark:hover:text-white",
              ]}
              aria-current={lang === languageLink.code ? "true" : undefined}
            >
              {languageLink.label}
            </a>
          ))
        }
      </div>
    </details>

    <details class="relative ml-auto md:hidden">
      <summary class="list-none rounded-lg border border-slate-300 bg-white/90 px-3 py-2 text-slate-700 marker:content-none dark:border-slate-700 dark:bg-slate-900/80 dark:text-slate-200">
        <span class="sr-only">{copy.openMenu}</span>
        <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </summary>

      <nav
        class="absolute right-0 mt-3 w-56 rounded-2xl border border-slate-300/80 bg-white/95 p-2 shadow-xl shadow-slate-900/10 dark:border-slate-700/80 dark:bg-slate-900/95 dark:shadow-slate-950/70"
        aria-label={copy.openMenu}
      >
        {
          navLinks.map((navigationLink) => {
            const isCurrentPage = basePathname === stripLanguagePrefix(normalizePathname(navigationLink.href));

            return (
              <a
                href={navigationLink.href}
                class:list={[
                  "block rounded-xl px-4 py-3 text-sm transition",
                  isCurrentPage
                    ? "bg-cyan-100 font-semibold text-cyan-700 dark:bg-cyan-500/15 dark:text-cyan-200"
                    : "text-slate-700 hover:bg-slate-100 hover:text-slate-900 dark:text-slate-300 dark:hover:bg-slate-800 dark:hover:text-white",
                ]}
                aria-current={isCurrentPage ? "page" : undefined}
              >
                {navigationLink.label}
              </a>
            );
          })
        }

        <details class="group mt-2 rounded-xl border border-slate-300/70 bg-white/75 dark:border-slate-700/70 dark:bg-slate-900/80">
          <summary class="flex list-none items-center justify-between px-4 py-3 text-sm text-slate-700 marker:content-none dark:text-slate-200">
            <span class="text-xs font-semibold tracking-[0.12em]">{currentLanguageLabel}</span>
            <svg
              class="h-3.5 w-3.5 text-slate-400 transition duration-200 group-open:rotate-180"
              viewBox="0 0 20 20"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="m6 8 4 4 4-4" />
            </svg>
          </summary>

          <div class="grid grid-cols-3 gap-2 px-3 pb-3">
            {
              languageSwitchLinks.map((languageLink) => (
                <a
                  href={languageLink.href}
                  class:list={[
                    "rounded-lg px-2 py-2 text-center text-xs font-semibold transition",
                    lang === languageLink.code
                      ? "bg-cyan-100 text-cyan-700 dark:bg-cyan-500/20 dark:text-cyan-200"
                      : "bg-slate-100 text-slate-700 hover:bg-slate-200 dark:bg-slate-800 dark:text-slate-300 dark:hover:bg-slate-700 dark:hover:text-white",
                  ]}
                  aria-current={lang === languageLink.code ? "true" : undefined}
                >
                  {languageLink.label}
                </a>
              ))
            }
          </div>
        </details>

        <div class="mt-3 border-t border-slate-200 pt-3 dark:border-slate-700">
          <ThemeToggleIsland client:load compact />
        </div>
      </nav>
    </details>
  </div>
</header>
